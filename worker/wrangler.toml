name = "nbbang-ocr"
main = "ocr-worker.js"
compatibility_date = "2024-01-01"

[vars]
ALLOWED_ORIGIN = "https://carrotjiwon-hub.github.io"

# 비밀 키는 이 파일에 넣지 않고 아래 명령으로 등록합니다:
#   wrangler secret put GOOGLE_VISION_API_KEY
